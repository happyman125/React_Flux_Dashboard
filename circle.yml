machine:
  node:
    version: 0.12.7
  services:
    - docker

dependencies:
  override:
    - npm install
  post:
    - npm run build
    - cp -r css $CIRCLE_ARTIFACTS

general:
  artifacts:
  - font
  - fonts
  - js/bundle.js
  - js/rainbowvis.js
  - index.html
